.git
.gitignore
.idea/
__pycache__/
*.pyc
*.pyo
*.pyd
.env        # Рекомендуется не включать .env в образ, а передавать переменные при запуске
venv/       # Если у вас есть локальное виртуальное окружение
app.log     # Файл логов не должен быть частью образа
*.xlsx      # Если вы не хотите, чтобы исходный Excel-файл был в образе, а монтировали его как volume
# Dockerfile  # Сам Dockerfile не нужно копировать в образ
# README.md