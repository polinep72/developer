# Changelog — WSB_core

Формат основан на Keep a Changelog и SemVer.

## [Unreleased]

## [1.0.4] - 2025-12-10

### Fixed
- Исправлено обрезание tooltip для кнопки "Настроить виджеты" в портале:
  - Tooltip выравнивается по левому краю кнопки для корректного отображения текста
  - Увеличена область отображения tooltip (`max-width: calc(100vw - 40px)`)
  - Добавлен `overflow: visible` для родительских контейнеров, чтобы tooltip не обрезался
- Исправлена совместимость `bcrypt` с `passlib`:
  - Понижена версия `bcrypt` с `4.3.0` до `3.2.2` для совместимости с `passlib 1.7.4`
  - Устранено предупреждение `AttributeError: module 'bcrypt' has no attribute '__about__'`

### Changed
- Обновлен `requirements.txt` портала: `bcrypt==3.2.2` (совместимость с `passlib`)

## [1.0.3] - 2025-12-09

### Added
- Добавлена логика подтверждения начала бронирования с таймаутом автоотмены:
  - При создании бронирования устанавливается статус `status='planned'`
  - При подтверждении начала брони через кнопку статус меняется на `status='active'`
  - Если бронирование не подтверждено в течение таймаута, оно автоматически отменяется (`cancel=TRUE`, `status='cancelled'`)
  - Автоотмена работает только для свежих бронирований (временной интервал не старше 20 минут)
- Добавлена логика автозавершения бронирований:
  - Если пользователь не нажал кнопку "Продлить" или "Нет, спасибо" в уведомлении об окончании, бронирование автоматически завершается через 10 минут
  - Автозавершение работает только для свежих бронирований (временной интервал не старше 20 минут)
- Добавлены кнопки "➕ Продлить" и "🚫 Нет, спасибо" в уведомлениях об окончании работы (Telegram)
- Добавлена обработка ошибок соединения с БД в `wsb_core/booking_watcher.py` и `wsb_bot/main.py`:
  - При разрыве соединения воркер не падает, пропускает цикл и продолжает работу
  - Ошибки соединения логируются как предупреждения

### Fixed
- Исправлена проблема с массовой автоотменой старых бронирований:
  - Автоотмена и автозавершение теперь работают только для свежих интервалов (не старше 20 минут)
  - Прошлые периоды не трогаются автоматическими процессами
- Исправлена проблема с дублированием уведомлений о начале бронирования:
  - Добавлена проверка времени начала бронирования перед отправкой уведомления
  - Уведомления не отправляются для бронирований, время которых уже прошло
- Исправлен календарь бронирований в портале:
  - Теперь показывает все бронирования, включая отмененные и завершенные, для полной истории
  - Убран фильтр `cancel = FALSE` из запроса календаря
- Исправлена обработка таймаутов Telegram API:
  - Бот автоматически перезапускает polling при таймауте чтения
  - Ошибки таймаута обрабатываются корректно без падения бота

### Changed
- Обновлена логика создания бронирований в `wsb_bot/services/booking_service.py`:
  - Новые бронирования создаются со статусом `status='planned'` (ожидают подтверждения)
  - При подтверждении начала брони статус меняется на `status='active'`
  - При отмене брони статус меняется на `status='cancelled'`
  - При завершении брони статус меняется на `status='finished'`
- Обновлена логика уведомлений в `wsb_bot/services/notification_worker.py`:
  - Уведомления о начале бронирования включают кнопку "✅ Подтвердить начало"
  - Уведомления об окончании включают кнопки "➕ Продлить" и "🚫 Нет, спасибо"
  - Добавлена проверка времени бронирования перед отправкой уведомлений
- Обновлена логика планирования уведомлений в `wsb_core/notifications_schedule.py`:
  - Не создаются записи в расписании для уведомлений, время которых уже в прошлом
  - Добавлена проверка статуса бронирования при планировании

## [1.0.2] - 2025-12-05

## [1.0.2] - 2025-12-05

### Fixed
- Исправлено дублирование уведомлений в Telegram-боте: при планировании уведомлений приходило 3 одинаковых уведомления вместо одного. Проблема была в логике сравнения времени задач в APScheduler - добавлена проверка с допуском в 1 секунду для учета микросекунд и небольших расхождений.
- Исправлены ошибки линтера в `wsb_bot/services/notification_service.py`:
  - Улучшена проверка типа для `localize` (используется `isinstance(scheduler_tz, pytz.BaseTzInfo)` вместо `hasattr`)
  - Добавлена проверка на `None` для `run_date` в `DateTrigger` перед использованием
  - Улучшена обработка исключений для `pytz`
- Обновлены версии всех компонентов после исправления бага (patch версии):
  - WSB_core: v.1.0.1 → v.1.0.2
  - WSB (бот): v.6.0.1 → v.6.0.2
  - WSB_portal: v.2.0.1 → v.2.0.2

## [1.0.1] - 2025-12-04

### Fixed
- Исправлена ошибка при завершении бронирования: слоты не освобождались при досрочном завершении работы на оборудовании. Теперь при завершении бронирования через `finish_booking_core` слоты в `wsb_time_slots` освобождаются от времени завершения (`finish`) до исходного времени окончания (`time_end`).
- Исправлены ошибки линтера в `wsb_bot/services/booking_service.py`:
  - Удалены некорректные проверки `rows_affected > 0` (при `fetch_results=False` метод возвращает `None` при успехе).
  - Добавлена проверка типа `equip_id` перед вызовом `check_booking_conflict`.
  - Исправлен формат параметров запроса (словарь преобразован в кортеж для `execute_query`).
- Обновлены версии всех компонентов после исправления бага (patch версии):
  - WSB_core: v.1.0.0 → v.1.0.1
  - WSB (бот): v.6.0.0 → v.6.0.1
  - WSB_portal: v.2.0.0 → v.2.0.1

## [1.0.0] - 2025-12-04

### Added
- Базовые версии проектов: WSB_core v.1.0.0, WSB v.6.0.0, WSB_portal v.2.0.0

### Added
- Базовый модуль `wsb_core/notifications_schedule.py` с описанием таблицы `wsb_notifications_schedule` и DDL для её инициализации (единое расписание уведомлений для бота и портала).

### Fixed
- Исправлена ошибка при завершении бронирования: слоты не освобождались при досрочном завершении работы на оборудовании. Теперь при завершении бронирования через `finish_booking_core` слоты в `wsb_time_slots` освобождаются от времени завершения (`finish`) до исходного времени окончания (`time_end`).
- Исправлены ошибки линтера в `wsb_bot/services/booking_service.py`:
  - Удалены некорректные проверки `rows_affected > 0` (при `fetch_results=False` метод возвращает `None` при успехе).
  - Добавлена проверка типа `equip_id` перед вызовом `check_booking_conflict`.
  - Исправлен формат параметров запроса (словарь преобразован в кортеж для `execute_query`).

## [0.2.0] - 2025-12-04

### Added
- Модуль `wsb_core/notifications_logic.py` с единой политикой уведомлений (интервалы до начала/конца, правила предложения продления).
- Логика продления в ядре `wsb_core/bookings_core.extend_booking_core` (права, рабочий день, лимиты, конфликты, обновление duration/extension).
- Интеграция продления портала и бота с ядром (`extend_booking_core`) с сохранением локального fallback в боте.
- Кнопка «Продлить» в портале на вкладке «Управление бронированием» с вызовом ядра продления.
- Кнопка «Завершить» в портале (между «Продлить» и «Отменить») только для активных броней, с учётом прав пользователя/админа.

### Changed
- Документация ядра обновлена с учётом продления и политики уведомлений (`PROJECT_CONTEXT.md`, `SOLUTIONS_HISTORY.md`, `CHAT_HISTORY.md`).
- Подвкладки «Истории бронирований» закреплены (position: sticky), чтобы оставаться видимыми при прокрутке.

## [0.1.0] - 2025-12-04
### Added
- Инициализация подпроекта `WSB_core` с базовыми файлами контекста и описанием целей.
- Скопированы все файлы из `WSB/` (Telegram‑бот) в `wsb_bot/`.
- Скопированы все файлы из `WSB_portal/` (веб‑портал) в `wsb_portal/`.
- Создана структура проекта с разделением на общее ядро (`wsb_core/`), бот (`wsb_bot/`) и портал (`wsb_portal/`).
- Боевые версии `WSB/` и `WSB_portal/` зафиксированы и больше не изменяются.
- Вся дальнейшая разработка ведётся только в `WSB_core/`.

### Changed
- Адаптированы импорты в `wsb_bot/services/booking_service.py` для использования `wsb_core.constants`.
- Обновлён `wsb_bot/constants.py` для использования констант из `wsb_core.constants`.
- Обновлён `wsb_portal/app/services/bookings.py` для использования констант из `wsb_core.constants`.
- Унифицированы константы времени работы: 7:00–22:00, шаг 30 минут, максимальная длительность 8 часов.
- Расширен `wsb_core/slots.py` функцией `calculate_available_slots_from_bookings()` для унифицированного расчёта слотов.
- Адаптирован `wsb_bot/services/booking_service.py` для использования `wsb_core.slots.calculate_available_slots_from_bookings()` вместо локальной реализации.
- Добавлен модуль `wsb_core/bookings_core.py` с общей бизнес‑логикой (создание/отмена, проверка конфликтов, синхронизация слотов, базовая поддержка продления).
- `wsb_portal/app/services/bookings.py` переведён на использование `wsb_core.bookings_core` для создания и отмены бронирований.
- Расширен `wsb_core/models.py`:
  - Добавлен статус `PLANNED` в `BookingStatus`.
  - Расширена модель `Booking` для соответствия структуре БД (добавлены поля: cancel, finish, time_interval, duration, data_booking, user_fio, equipment_name, date).
  - Добавлена функция `determine_booking_status()` для определения статуса брони на основе полей БД.
  - Добавлена функция `booking_from_db_row()` для создания объекта Booking из строки БД с поддержкой разных вариантов именования полей.
- Обновлена документация: README.md, PROJECT_CONTEXT.md с описанием текущей структуры.
