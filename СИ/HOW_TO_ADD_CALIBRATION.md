# Как добавить новую поверку/аттестацию

## 🎯 Автоматический расчет в БД

Система автоматически рассчитывает даты при добавлении поверки в базу данных.

## 📝 Что нужно ввести

### При добавлении через SQL:
```sql
INSERT INTO calibration_certificates (
    equipment_id,           -- ID оборудования из таблицы equipment
    certificate_number,     -- Номер свидетельства о поверке/аттестации
    certificate_date,       -- Дата проведения поверки/аттестации
    calibration_cost,       -- Стоимость (опционально)
    certificate_url         -- URL сертификата (опционально)
) VALUES (1, 'ПОВ-2024-001', '2024-06-15', 1500.00, 'https://example.com/cert.pdf');
```

### При добавлении через функцию:
```sql
SELECT add_calibration_certificate(
    1,                      -- equipment_id
    'ПОВ-2024-001',         -- certificate_number  
    '2024-06-15',           -- certificate_date
    1500.00,                -- calibration_cost (опционально)
    'https://example.com/cert.pdf'  -- certificate_url (опционально)
);
```

## 🤖 Что рассчитывается автоматически

| Поле | Формула | Описание |
|------|---------|----------|
| `next_calibration_date` | `certificate_date + (mpi * 365) - 1 день` | Дата следующей поверки |
| `days_until_calibration` | `next_calibration_date - CURRENT_DATE` | Дней до поверки |

## 📊 Пример работы

### Вводим в БД:
```sql
INSERT INTO calibration_certificates (
    equipment_id, certificate_number, certificate_date, calibration_cost, certificate_url
) VALUES (1, 'ПОВ-2024-001', '2024-06-15', 1500.00, 'https://example.com/cert.pdf');
```

### БД автоматически рассчитает:
- **МПИ**: 1 год (из таблицы equipment)
- **next_calibration_date**: 2025-06-14
- **days_until_calibration**: 350 (если сегодня 2024-06-30)

## 🔧 Как найти equipment_id

```sql
SELECT id, name, serial_number 
FROM equipment 
WHERE name LIKE '%название%' 
   OR serial_number = 'заводской_номер';
```

## ✅ Проверка результата

```sql
SELECT 
    certificate_number,
    certificate_date,
    next_calibration_date,
    days_until_calibration,
    calibration_cost
FROM calibration_certificates 
WHERE certificate_number = 'ПОВ-2024-001';
```

## 🌐 Отображение в веб-интерфейсе

- **История поверок** сохраняется в БД
- **В таблице отображается** только последняя поверка по дате
- **Статус обновляется** автоматически при каждом обновлении страницы

## 🎯 Преимущества

1. **Автоматизация** - не нужно вручную рассчитывать даты
2. **Точность** - расчеты основаны на МПИ из БД
3. **История** - все поверки сохраняются
4. **Актуальность** - статус всегда соответствует текущей дате

## 📋 Порядок действий

1. **Найти ID оборудования** в таблице equipment
2. **Ввести данные поверки** (только основные поля)
3. **БД автоматически рассчитает** даты и статус
4. **Проверить результат** в веб-интерфейсе

Готово! Система работает автоматически. 🚀
