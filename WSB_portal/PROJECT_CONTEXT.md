# WSB Portal — веб-приложение бронирования

## Описание
Подпроект `WSB_portal` — веб-версия системы WorkSpace Booking. Он публикуется внутри сети по адресу `http://192.168.1.139:8090` и повторяет логику Telegram-бота WSB, предоставляя полноценный браузерный интерфейс для пользователей и администраторов.

## Основные компоненты
- **app/main.py** — FastAPI-приложение, маршруты для всех вкладок и API-эндпоинты
- **app/templates/** — HTML-шаблоны (index.html, login.html, reset_password.html)
- **app/static/** — стили (CSS), JavaScript логика вкладок и API, изображения (логотип)
- **app/services/heatmap.py** — получение данных из БД RM и построение тепловой карты
- **app/services/dashboard.py** — сбор статистики по бронированиям, построение графиков и таблиц
- **app/services/bookings.py** — логика бронирования (категории, оборудование, слоты, создание/отмена)
- **app/services/equipment.py** — работа с БД equipment (СИ, ИО, ВО, Госреестр)
- **app/services/auth.py** — авторизация пользователей (JWT, пароли, восстановление)
- **app/services/users.py** — управление пользователями (создание, редактирование, удаление, сброс паролей)
- **requirements.txt** — зависимости (FastAPI, Plotly, psycopg, numpy, passlib, python-jose)

## Назначение подпроекта
- Обеспечить веб-интерфейс бронирования рабочих мест, оборудования и уведомлений
- Синхронизировать данные с текущей PostgreSQL базой, используемой ботами WSB
- Интегрировать heatmap визуализацию и отчеты на одной площадке

## Связи с другими подпроектами
- `WSB` и `telegram_bot_WorkSpaceBooking` — источник бизнес-логики и моделей
- `NewBot` — совместимые подходы к уведомлениям и расписанию
- `WSB/heatmap_app` — планируемая интеграция компонента визуализации

## Текущий статус
- ✅ Реализован FastAPI сервер с шаблоном, статикой и полным набором API-эндпоинтов
- ✅ **Вкладка 1: «Бронирование оборудования»** — форма бронирования с зависимыми селектами (категория → оборудование → время → длительность), сводка «Активные брони / Свободные слоты», тепловая карта занятости со светлоголубой сеткой
- ✅ **Вкладка 2: «Управление бронированием»** — таблица бронирований с фильтрацией по дате, раздельный доступ для пользователей (только свои) и администраторов (все), функционал отмены бронирований
- ✅ **Вкладка 3: «История бронирований (статистика и графики)»** — дашборд с фильтрами, графиками относительной/абсолютной загрузки, таблицами пользователей/оборудования, историческая тепловая карта (по умолчанию «вчера», без легенды)
- ✅ **Вкладка 4: «Система учета оборудования (СИ)»** — полная интеграция модуля СИ с 4 подвкладками (СИ, ИО, ВО, Госреестр), функционал добавления оборудования, поддержка HTML-тегов и кликабельных ссылок
- ✅ **Вкладка 5: «Управление пользователями»** — создание, редактирование, удаление пользователей, сброс паролей (только для администраторов)
- ✅ Система авторизации пользователей (JWT, вход по телефону/email, смена и восстановление пароля)
- ✅ Регистрация новых пользователей через веб-интерфейс с использованием Telegram ID
- ✅ Система уведомлений о бронированиях (email-уведомления при создании/отмене, уведомления администраторам о конфликтах, настройки уведомлений в профиле пользователя)
- ✅ Планировщик напоминаний о начале работы (APScheduler) с групповыми письмами за 15 минут до старта и журналированием отправок
- ✅ Фиксированная шапка и навигационные вкладки при прокрутке страницы
- ✅ Графики Plotly и тепловые карты автоматически растягиваются на ширину контейнера, блоки тепловой карты квадратные
- ✅ Шапка портала использует фирменный логотип ИПК «Электрон-Маш»
- ✅ Heatmap, дашборд и бронирования берут данные из БД RM; модуль СИ работает с БД equipment; при отсутствии кредов работает демо-режим
- ✅ Реализовано кеширование данных в Redis (тепловые карты, дашборд, каталоги оборудования, модуль СИ) с автоматической инвалидацией при изменениях
- ✅ Оптимизированы запросы к БД: скрипт `scripts/create_indexes.py` теперь создаёт индексы как для RM (bookings), так и для БД `equipment`; выборки модуля СИ запрашивают только используемые поля

## Известные задачи

### ✅ Завершенные
- ✅ Реализовать регистрацию новых пользователей через веб-интерфейс
- ✅ Добавить уведомления о бронированиях (email/SMS)
- ✅ Реализовать кеширование данных в Redis
- ✅ Оптимизировать запросы к БД (индексы, фильтрация на уровне SQL)
- ✅ Добавить экспорт бронирований и дашборда в CSV
- ✅ Унифицировать стили кнопок и цветовую схему (#027BBC)

### 🔴 Критический приоритет
- Безопасность (rate limiting, CSRF-защита, валидация)
- ✅ Docker и автоматизированный деплой (Dockerfile, docker-compose) — реализовано

### 🟡 Высокий приоритет
- Экспорт отчетов в Excel/PDF (расширение текущего CSV)
- Сопоставление оборудования между БД RM и equipment
- Фильтры и поиск в модуле СИ

### 🟢 Средний приоритет
- Календарь бронирований
- Интеграция с Telegram-ботом для синхронизации данных
- Логирование и мониторинг (структурированное логирование, метрики)
- Тестирование (unit, интеграционные, E2E)
- API документация (Swagger/OpenAPI)

### 🔵 Низкий приоритет
- Расширенное управление пользователями (массовые операции, фильтры)
- Повторяющиеся бронирования
- История изменений бронирований
- Визуальные улучшения (темная тема, анимации)

**Подробный список всех задач с описанием и оценкой трудозатрат см. в `PROJECT_AUDIT.md`.**

## Архитектурные решения
- **Мобильная версия:** Отдельная мобильная веб-версия не разрабатывается. Telegram-бот покрывает мобильные сценарии (быстрое бронирование, уведомления). Веб-портал оптимизирован для десктопов и планшетов, базовая адаптивность достаточна для больших экранов. Разделение ролей: Telegram-бот для быстрых действий, веб-портал для аналитики и управления.

## Ключевые настройки
- `PORTAL_HOST` (192.168.1.139) и `PORTAL_PORT` (8090)
- Доступы к PostgreSQL задаются через `.env`:
  - БД RM: `POSTGRE_HOST`, `POSTGRE_PORT`, `POSTGRE_USER`, `POSTGRE_PASSWORD`, `POSTGRE_DBNAME=RM`
  - БД equipment: `EQUIPMENT_DB_NAME=equipment`, `EQUIPMENT_DB_HOST`, `EQUIPMENT_DB_USER`, `EQUIPMENT_DB_PASSWORD`
- Redis-кеш: `REDIS_HOST`, `REDIS_PORT`, `REDIS_DB`, `REDIS_PASSWORD` (по умолчанию 256 MB, TTL настраивается внутри сервисов), глобальный флаг `ENABLE_CACHE`
- SMTP для уведомлений: `SMTP_HOST`, `SMTP_PORT`, `SMTP_USER`, `SMTP_PASSWORD`, `SMTP_FROM_EMAIL`, `SMTP_FROM_NAME`, `ENABLE_EMAIL`
- Планировщик напоминаний использует APScheduler; параметры времени берутся из системного времени сервера
- JWT-секрет для авторизации: `AUTH_SECRET_KEY` (по умолчанию генерируется случайный)
- Параметры CORS и авторизации настроены для работы внутри сети

