# SOLUTIONS_HISTORY.md

## История решенных проблем и решений

### 2025-11-26

#### Проблема: Автоматический перезапуск остановленных контейнеров
**Описание:** При перезагрузке сервера некоторые Docker-контейнеры не стартуют автоматически.

**Решение:**
- Добавлена функция `_attempt_restart_container()` для перезапуска контейнеров
- Добавлена проверка `_is_restart_allowed()` для контроля автоперезапуска
- Реализована логика автоперезапуска в `check_docker_container_status()`
- Добавлены переменные окружения `AUTO_RESTART_CONTAINERS` и `AUTO_RESTART_ALLOW_LIST`

**Файлы:** `main.py`

---

#### Проблема: Улучшение конфигурации Docker
**Описание:** Предупреждения при сборке Docker-образа и docker-compose.

**Решение:**
- Исправлен формат `ENV PYTHONUNBUFFERED=1` в Dockerfile
- Удален устаревший ключ `version: '3.8'` из docker-compose.yml
- Создан файл `.dockerignore` для оптимизации сборки

**Файлы:** `Dockerfile`, `docker-compose.yml`, `.dockerignore`

---

#### Проблема: Добавление Telegram-бота для ручных проверок
**Описание:** Необходимость вручную проверять статус сервисов в любой момент.

**Решение:**
- Интегрирован `python-telegram-bot` для создания интерактивного бота
- Добавлены команды: `/start`, `/status`, `/check`, `/reload`, `/db`
- Реализованы инлайн-кнопки для быстрого доступа
- Бот запускается в отдельном потоке с `asyncio`

**Файлы:** `main.py`, `requirements.txt`

---

#### Проблема: Миграция конфигурации сервисов из .env в БД
**Описание:** Конфигурация сервисов хранилась в .env, что требовало пересборки контейнера при изменениях.

**Решение:**
- Создана таблица `monitoring_services` в PostgreSQL
- Реализована функция `load_services_from_db()` для динамической загрузки
- Добавлена функция `init_database()` с миграцией из .env
- Упрощен `.env` — только параметры подключения к БД и Telegram
- Добавлена команда `/reload` для перезагрузки конфигурации без перезапуска

**Файлы:** `main.py`

---

#### Проблема: Telegram-бот не отвечает на команды
**Описание:** Бот запускался, но не обрабатывал команды.

**Решение:**
- Добавлен `await TELEGRAM_APP.updater.start_polling()` в `start_telegram_bot()`
- Исправлен запуск бота в отдельном потоке с `loop.run_forever()`
- Обеспечен правильный запуск asyncio event loop

**Файлы:** `main.py`

---

#### Проблема: Ошибки при загрузке сервисов из БД
**Описание:** `NameError` и проблемы с порядком инициализации.

**Решение:**
- Переупорядочен код: `get_db_connection()` и `init_database()` определены до использования
- Изменен критический выход на предупреждение при отсутствии сервисов
- Исправлена обработка JSON в `load_services_from_db()` (поддержка dict и str)
- Улучшена обработка ошибок с использованием `logger`

**Файлы:** `main.py`

---

#### Проблема: Перезапуск контейнеров через Telegram-бота
**Описание:** Необходимость перезапускать контейнеры вручную через Docker CLI.

**Решение:**
- Добавлена функция `restart_container()` для перезапуска контейнеров
- Добавлена функция `get_monitored_containers()` для получения списка контейнеров
- Реализована команда `/restart` с инлайн-клавиатурой выбора контейнера
- Добавлена кнопка "🔄 Перезапустить контейнер" в главное меню
- Реализован обработчик callback для перезапуска выбранного контейнера

**Файлы:** `main.py`

---

#### Проблема: Отсутствие команды /help
**Описание:** Пользователи не знали о доступных командах бота.

**Решение:**
- Добавлен обработчик `help_command()` с описанием всех команд
- Зарегистрирована команда `/help` в боте
- Добавлено описание кнопок меню в справке

**Файлы:** `main.py`

---

#### Проблема: Устаревшая документация
**Описание:** README.md не отражал текущую функциональность (перезапуск контейнеров, команда /help).

**Решение:**
- Обновлен раздел "Telegram-бот" с полным списком команд
- Добавлено описание кнопок главного меню
- Обновлен раздел "Авто‑рестарт контейнеров" с информацией о ручном перезапуске
- Исправлен раздел "Добавление новых сервисов" (убрана устаревшая информация о .env)
- Обновлены разделы "Возможности" и "Описание проекта"

**Файлы:** `README.md`

---

## Паттерны решений

1. **Динамическая конфигурация через БД** — все сервисы настраиваются без пересборки
2. **Интерактивный бот** — удобное управление через Telegram с кнопками
3. **Автоматизация** — автоперезапуск контейнеров и автоматические проверки
4. **Модульность** — четкое разделение функций мониторинга, БД и бота

