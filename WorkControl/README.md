## WorkControl — мониторинг сервисов с Telegram-ботом и БД-конфигурацией

### Кратко
- Конфигурация сервисов хранится в БД PostgreSQL (`monitoring.monitoring_services`).
- В `.env` — только параметры подключения к БД и Telegram.
- Пересборка контейнера не требуется при добавлении/изменении сервисов — достаточно записать их в БД и выполнить `/reload` в боте (или перезапустить контейнер).
- Управление контейнерами через Telegram-бота: перезапуск, проверка статуса, просмотр отчетов.

### Требования
- Docker + Docker Compose
- PostgreSQL (доступ к БД `monitoring`)

### Установка и запуск
1) Создать `.env` (минимально необходимый набор):
```
TELEGRAM_BOT_TOKEN=<токен>
TELEGRAM_CHAT_ID=<chat_id>
TELEGRAM_BOT_ENABLED=true
HTTP_TIMEOUT=5
REPORT_TIME=07:30
CHECK_INTERVAL_HOURLY_AT=:05
AUTO_RESTART_CONTAINERS=false
DB_ENABLED=true
DB_USER=postgres
DB_PASSWORD=<пароль>
DB_HOST=<ip_или_dns>
DB_PORT=5432
DB_NAME=monitoring
```
Все сервисы (URL и контейнеры) берутся ИСКЛЮЧИТЕЛЬНО из БД `monitoring_services`.

2) Собрать и запустить:
```
docker compose up -d --build monitor
```

3) Проверить логи:
```
docker logs -f my-monitor
```

### Структура БД и инициализация
Приложение само создаёт таблицы при старте, если `DB_ENABLED=true`:
```
CREATE TABLE IF NOT EXISTS monitoring_services (
  id SERIAL PRIMARY KEY,
  name VARCHAR(255) NOT NULL UNIQUE,
  type VARCHAR(50) NOT NULL,            -- 'http' | 'docker_container_status'
  check_params JSONB,                   -- для http: {url, expected_status, timeout}; для docker: {container_name}
  enabled BOOLEAN DEFAULT TRUE,
  priority INTEGER DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Добавление сервисов (без пересборки)
Вставьте записи в `monitoring_services`, затем выполните `/reload` в Telegram-боте.

Примеры:
```
-- Docker контейнеры
INSERT INTO monitoring_services (name, type, check_params, priority)
VALUES
 ('Docker: WSB Bot', 'docker_container_status', '{"container_name": "wsb_bot_container"}', 1),
 ('Docker: HMCB Bot', 'docker_container_status', '{"container_name": "telegram_bot_hmcb_app-1"}', 2),
 ('Docker: CRB Bot',  'docker_container_status', '{"container_name": "crb_bot_container"}', 3),
 ('Docker: PDF Converter', 'docker_container_status', '{"container_name": "pdf2md-test"}', 4);

-- WEB сервисы
INSERT INTO monitoring_services (name, type, check_params, priority)
VALUES
 ('WEB: CRB Heatmap', 'http', '{"url":"http://192.168.1.139:8081/", "expected_status":200, "timeout":5}', 5),
 ('WEB: WSB Heatmap', 'http', '{"url":"http://192.168.1.139:8082/", "expected_status":200, "timeout":5}', 6),
 ('WEB: Dashboard WSB', 'http', '{"url":"http://192.168.1.139:8083/", "expected_status":200, "timeout":5}', 7),
 ('WEB: Система учета измерительного оборудования', 'http', '{"url":"http://192.168.1.139:8084/", "expected_status":200, "timeout":5}', 8),
 ('WEB: ChipID IP Parser', 'http', '{"url":"http://192.168.1.139:8085/", "expected_status":200, "timeout":5}', 9),
 ('WEB: Zotero Loader', 'http', '{"url":"http://192.168.1.139:8086/", "expected_status":200, "timeout":5}', 10),
 ('WEB: Crystal Stock', 'http', '{"url":"http://192.168.1.139:8087/", "expected_status":200, "timeout":5}', 11),
 ('WEB: PDF Converter', 'http', '{"url":"http://192.168.1.139:8501/", "expected_status":200, "timeout":5}', 12);
```

Включение/выключение сервиса:
```
UPDATE monitoring_services SET enabled=false WHERE name='WEB: PDF Converter';
UPDATE monitoring_services SET enabled=true  WHERE name='WEB: PDF Converter';
```

Изменение параметров:
```
UPDATE monitoring_services
SET check_params = jsonb_set(check_params, '{timeout}', '10'::jsonb)
WHERE name='WEB: PDF Converter';
```

После изменений выполните перезагрузку конфигурации:
```
— через бота: /reload
— или перезапуск контейнера: docker restart my-monitor
```

### Telegram-бот
Доступные команды:
- `/start` — главное меню с кнопками быстрого доступа
- `/status` — показать текущий статус всех сервисов
- `/check` — выполнить полную проверку всех сервисов
- `/restart` — перезапустить Docker-контейнер (выбор из списка)
- `/reload` — перезагрузить конфигурацию из базы данных
- `/db` — проверить новые записи в базе данных (за последние 12 часов)
- `/help` — показать справку по всем командам

**Кнопки в главном меню:**
- 🔍 **Проверить сервисы** — выполнить проверку всех сервисов
- 📊 **Статус** — показать текущий статус
- 🔄 **Перезапустить контейнер** — меню выбора контейнера для перезапуска
- 🔄 **Обновить** — вернуться в главное меню

Форматирование сообщений — MarkdownV2 (где применимо).

### Авто‑рестарт контейнеров
Опционально: `AUTO_RESTART_CONTAINERS=true` в `.env` включает попытку авто‑рестарта для упавших контейнеров из списка сервисов типа `docker_container_status`.

**Ручной перезапуск через бота:**
- Используйте команду `/restart` или кнопку "🔄 Перезапустить контейнер" в главном меню
- Выберите нужный контейнер из списка
- Контейнер будет перезапущен, результат отобразится в сообщении

### Полезные команды
```
docker compose up -d --build monitor   # сборка и запуск
docker logs -f my-monitor              # логи
docker restart my-monitor              # перезапуск
```

# WorkControl - Система мониторинга сервисов

## Описание проекта

WorkControl - это система автоматического мониторинга состояния различных сервисов с отправкой уведомлений в Telegram и интерактивным управлением через бота. Система отслеживает:

- **Telegram боты** - проверка статуса Docker контейнеров
- **Веб-сервисы** - проверка доступности HTTP endpoints
- **Автоматические уведомления** о проблемах и восстановлении сервисов
- **Ежедневные отчеты** о состоянии всех сервисов
- **Управление контейнерами** - перезапуск Docker-контейнеров через Telegram-бота
- **База данных PostgreSQL** - хранение конфигурации сервисов и истории проверок

## Возможности

### 🔍 Мониторинг сервисов
- **Docker контейнеры**: Проверка статуса и состояния здоровья контейнеров
- **HTTP endpoints**: Проверка доступности веб-сервисов с настраиваемыми таймаутами
- **Автоматические уведомления**: Мгновенные уведомления при проблемах и восстановлении
- **Управление контейнерами**: Перезапуск Docker-контейнеров через Telegram-бота

### 📊 Отчетность
- **Ежечасные проверки**: Регулярный мониторинг всех сервисов
- **Ежедневные отчеты**: Подробные отчеты о состоянии всех сервисов
- **Детальное логирование**: Полная история всех проверок и событий

### 🔧 Гибкая настройка
- **Конфигурация через БД**: Все сервисы настраиваются через PostgreSQL без пересборки
- **Конфигурация через .env**: Параметры подключения и общие настройки
- **Настраиваемое расписание**: Гибкая настройка времени проверок и отчетов
- **Модульная архитектура**: Легкое добавление новых типов проверок
- **Интерактивный бот**: Удобное управление через Telegram с кнопками и командами

## Архитектура

```
WorkControl/
├── main.py              # Основной модуль мониторинга
├── requirements.txt      # Python зависимости
├── Dockerfile           # Конфигурация Docker образа
├── docker-compose.yml   # Docker Compose конфигурация
└── .env                 # Переменные окружения (не включен в репозиторий)
```

## Установка и запуск

### Предварительные требования

- Python 3.9+
- Docker и Docker Compose
- Telegram Bot Token
- Telegram Chat ID для уведомлений

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd WorkControl
```

### 2. Настройка переменных окружения

Создайте файл `.env` (минимально необходимый набор). Все сервисы (URL и контейнеры) берутся только из БД `monitoring_services`:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here
TELEGRAM_BOT_ENABLED=true
HTTP_TIMEOUT=5
REPORT_TIME=07:30
CHECK_INTERVAL_HOURLY_AT=:05
AUTO_RESTART_CONTAINERS=false

DB_ENABLED=true
DB_USER=postgres
DB_PASSWORD=your_password_here
DB_HOST=your_db_host
DB_PORT=5432
DB_NAME=monitoring
```

### 3. Запуск с Docker Compose

```bash
# Сборка и запуск
docker compose up -d --build monitor

# Просмотр логов
docker logs -f my-monitor

# Остановка
docker compose down
```

### 4. Запуск локально (без Docker)

```bash
# Установка зависимостей
pip install -r requirements.txt

# Запуск
python main.py
```

## Конфигурация

### Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | Обязательно |
| `TELEGRAM_CHAT_ID` | ID чата для уведомлений | Обязательно |
| `HTTP_TIMEOUT` | Таймаут HTTP запросов (сек) | 5 |
| `REPORT_TIME` | Время ежедневного отчета | 07:30 |
| `CHECK_INTERVAL_HOURLY_AT` | Минута для ежечасных проверок | :05 |
| `LOG_LEVEL` | Уровень логирования | INFO |

### Добавление новых сервисов

**Все сервисы добавляются через базу данных PostgreSQL**, а не через `.env` или код.

См. раздел "Добавление сервисов (без пересборки)" выше для примеров SQL-запросов.

## Мониторинг сервисов

### Поддерживаемые типы проверок

1. **Docker Container Status**
   - Проверка статуса контейнера (running/stopped)
   - Проверка состояния здоровья (healthy/unhealthy)
   - Автоматическое обнаружение проблем

2. **HTTP Endpoint**
   - Проверка доступности URL
   - Проверка HTTP статус кода
   - Настраиваемые таймауты
   - Обработка различных сетевых ошибок

### Уведомления

Система отправляет уведомления в Telegram при:

- 🚨 **Проблемах**: Сервис стал недоступен
- ✅ **Восстановлении**: Сервис снова работает
- 📅 **Ежедневных отчетах**: Статус всех сервисов

## Логирование

Система ведет подробные логи всех операций:

- Уровень логирования настраивается через `LOG_LEVEL`
- Логи включают время, уровень, модуль и сообщение
- Поддержка различных уровней: DEBUG, INFO, WARNING, ERROR, CRITICAL

## Безопасность

- Все чувствительные данные хранятся в переменных окружения
- Файл `.env` не включен в репозиторий
- Используется экранирование Markdown для безопасной отправки сообщений

## Разработка

### Структура кода

- **Основной модуль**: `main.py` содержит всю логику мониторинга
- **Модули проверок**: Функции для проверки Docker и HTTP сервисов
- **Модуль уведомлений**: Отправка сообщений в Telegram
- **Модуль отчетов**: Генерация ежедневных отчетов

### Добавление новых типов проверок

Для добавления нового типа проверки:

1. Создайте функцию проверки
2. Добавьте новый тип в `check_service()`
3. Обновите конфигурацию в `SERVICES_TO_MONITOR`

## Устранение неполадок

### Частые проблемы

1. **Docker API недоступен**
   - Проверьте права доступа к Docker socket
   - Убедитесь, что Docker демон запущен

2. **Telegram уведомления не отправляются**
   - Проверьте правильность `TELEGRAM_BOT_TOKEN`
   - Убедитесь, что бот добавлен в чат

3. **HTTP проверки не работают**
   - Проверьте доступность URL
   - Увеличьте `HTTP_TIMEOUT` при необходимости

### Просмотр логов

```bash
# Docker Compose
docker compose logs -f monitor

# Локальный запуск
python main.py 2>&1 | tee monitor.log
```

## Лицензия

Этот проект распространяется под лицензией MIT.

## Настройка уведомлений для других пользователей

### Вариант 1: Общий групповой чат

**Самый простой способ:**
1. Создайте групповой чат в Telegram
2. Добавьте туда всех пользователей, которые должны получать уведомления
3. Добавьте бота в группу
4. Получите ID группы и настройте в `.env`:

```env
TELEGRAM_CHAT_ID=-1001234567890  # ID группового чата
```

### Вариант 2: Индивидуальные уведомления

**Для отправки уведомлений разным пользователям:**

1. **Создайте отдельного бота** для каждого пользователя
2. **Настройте переменные окружения:**

```env
# Основной бот (для администратора)
TELEGRAM_BOT_TOKEN=admin_bot_token
TELEGRAM_CHAT_ID=admin_chat_id

# Дополнительные боты для других пользователей
TELEGRAM_BOT_TOKEN_USER1=user1_bot_token
TELEGRAM_CHAT_ID_USER1=user1_chat_id

TELEGRAM_BOT_TOKEN_USER2=user2_bot_token
TELEGRAM_CHAT_ID_USER2=user2_chat_id
```

3. **Модифицируйте код** для отправки уведомлений нескольким пользователям:

```python
# В main.py добавьте функцию для множественной отправки
def send_telegram_message_multiple(message_text: str, use_markdown: bool = True):
    """Отправка сообщения нескольким пользователям"""
    recipients = [
        {"token": TELEGRAM_BOT_TOKEN, "chat_id": TELEGRAM_CHAT_ID},
        {"token": os.getenv("TELEGRAM_BOT_TOKEN_USER1"), "chat_id": os.getenv("TELEGRAM_CHAT_ID_USER1")},
        {"token": os.getenv("TELEGRAM_BOT_TOKEN_USER2"), "chat_id": os.getenv("TELEGRAM_CHAT_ID_USER2")},
    ]
    
    for recipient in recipients:
        if recipient["token"] and recipient["chat_id"]:
            try:
                send_telegram_message_to_recipient(message_text, recipient["token"], recipient["chat_id"], use_markdown)
            except Exception as e:
                logger.error(f"Ошибка отправки пользователю {recipient['chat_id']}: {e}")

def send_telegram_message_to_recipient(message_text: str, token: str, chat_id: str, use_markdown: bool = True):
    """Отправка сообщения конкретному получателю"""
    url = f"https://api.telegram.org/bot{token}/sendMessage"
    payload = {"chat_id": chat_id, "text": message_text}
    if use_markdown: 
        payload["parse_mode"] = "MarkdownV2"
    
    response = requests.post(url, data=payload, timeout=10)
    response.raise_for_status()
```

### Вариант 3: Веб-интерфейс для просмотра статуса

**Для доступа через браузер:**

1. **Добавьте веб-сервер** в проект:

```python
# Добавьте в requirements.txt
flask==2.3.3

# Создайте web_interface.py
from flask import Flask, render_template
import json

app = Flask(__name__)

@app.route('/status')
def status():
    return json.dumps(SERVICE_STATUSES, indent=2, default=str)

@app.route('/')
def dashboard():
    return render_template('dashboard.html', services=SERVICE_STATUSES)

if __name__ == '__main__':
    app.run(host='0.0.0.0', port=5000)
```

2. **Создайте HTML шаблон** для веб-интерфейса:

```html
<!-- templates/dashboard.html -->
<!DOCTYPE html>
<html>
<head>
    <title>WorkControl - Статус сервисов</title>
    <meta http-equiv="refresh" content="30">
    <style>
        .service { margin: 10px; padding: 10px; border: 1px solid #ccc; }
        .ok { background-color: #d4edda; }
        .fail { background-color: #f8d7da; }
        .unknown { background-color: #fff3cd; }
    </style>
</head>
<body>
    <h1>Статус сервисов WorkControl</h1>
    <div id="services">
        {% for name, data in services.items() %}
        <div class="service {{ data.status.lower() }}">
            <h3>{{ name }}</h3>
            <p>Статус: <strong>{{ data.status }}</strong></p>
            <p>Последнее сообщение: {{ data.last_message }}</p>
            <p>Время последнего события: {{ data.last_event_time }}</p>
        </div>
        {% endfor %}
    </div>
</body>
</html>
```

3. **Обновите docker-compose.yml:**

```yaml
services:
  monitor:
    # ... существующая конфигурация ...
    ports:
      - "5000:5000"  # Веб-интерфейс
```

### Вариант 4: API для интеграции

**Для программного доступа к статусу:**

```python
# Добавьте API endpoints в web_interface.py
@app.route('/api/status')
def api_status():
    return json.dumps(SERVICE_STATUSES, indent=2, default=str)

@app.route('/api/status/<service_name>')
def api_service_status(service_name):
    if service_name in SERVICE_STATUSES:
        return json.dumps(SERVICE_STATUSES[service_name], indent=2, default=str)
    return json.dumps({"error": "Service not found"}), 404

@app.route('/api/health')
def api_health():
    total = len(SERVICE_STATUSES)
    ok_count = sum(1 for s in SERVICE_STATUSES.values() if s['status'] == 'OK')
    return json.dumps({
        "total_services": total,
        "healthy_services": ok_count,
        "unhealthy_services": total - ok_count,
        "health_percentage": (ok_count / total * 100) if total > 0 else 0
    })
```

### Вариант 5: Email уведомления

**Для отправки уведомлений на email:**

```python
# Добавьте в requirements.txt
smtplib  # встроенная библиотека

# Функция отправки email
def send_email_notification(subject: str, message: str):
    import smtplib
    from email.mime.text import MIMEText
    
    # Настройки SMTP
    smtp_server = os.getenv("SMTP_SERVER", "smtp.gmail.com")
    smtp_port = int(os.getenv("SMTP_PORT", "587"))
    email_user = os.getenv("EMAIL_USER")
    email_password = os.getenv("EMAIL_PASSWORD")
    email_recipients = os.getenv("EMAIL_RECIPIENTS", "").split(",")
    
    if not all([email_user, email_password, email_recipients]):
        logger.warning("Email настройки неполные, пропускаем отправку")
        return
    
    msg = MIMEText(message)
    msg['Subject'] = subject
    msg['From'] = email_user
    msg['To'] = ", ".join(email_recipients)
    
    try:
        with smtplib.SMTP(smtp_server, smtp_port) as server:
            server.starttls()
            server.login(email_user, email_password)
            server.send_message(msg)
        logger.info(f"Email отправлен: {subject}")
    except Exception as e:
        logger.error(f"Ошибка отправки email: {e}")
```

**Настройка в .env:**
```env
# Email настройки
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
EMAIL_USER=your_email@gmail.com
EMAIL_PASSWORD=your_app_password
EMAIL_RECIPIENTS=user1@example.com,user2@example.com
```

## Рекомендации по выбору метода

| Метод | Сложность | Доступность | Рекомендация |
|-------|-----------|-------------|--------------|
| **Групповой чат** | 🟢 Просто | 🟢 Высокая | Для небольших команд |
| **Индивидуальные боты** | 🟡 Средне | 🟢 Высокая | Для точного контроля |
| **Веб-интерфейс** | 🟡 Средне | 🟢 Высокая | Для технических пользователей |
| **API** | 🟡 Средне | 🟢 Высокая | Для интеграции с другими системами |
| **Email** | 🟡 Средне | 🟡 Средняя | Для критически важных уведомлений |

## Поддержка

Для получения поддержки или сообщения о проблемах, создайте issue в репозитории проекта. 
