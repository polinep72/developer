# ChipDip Parser & Analytics

Система парсинга товаров с сайта ChipDip с веб‑интерфейсом для аналитики, интерактивными графиками (Plotly) и экспортом в Excel/PowerPoint.

## 🚀 Возможности

- **Парсинг товаров**: Автоматический сбор данных о товарах с сайта ChipDip
- **Веб-интерфейс**: Flask приложение для статистики и управления товарами
- **Планировщик**: Умный планировщик с учетом рабочих часов и праздников
- **Docker**: Полная контейнеризация для простого развертывания
- **PostgreSQL**: Надежное хранение данных с историей изменений

## 📋 Требования

- Docker и Docker Compose
- Python 3.11+ (для локальной разработки)
- PostgreSQL (включен в Docker)

## 🛠 Установка и запуск

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd ChipDip
```

### 2. Настройка конфигурации
```bash
# Скопируйте пример конфигурации
cp env.example .env

# Отредактируйте .env файл с вашими настройками
nano .env
```

### 3. Запуск через Docker Compose
```bash
# Сборка и запуск всех сервисов
docker-compose up -d --build

# Просмотр логов
docker-compose logs -f

# Остановка сервисов
docker-compose down
```

### 4. Доступ к приложению и health
- **Веб-интерфейс**: `http://localhost:8085`
- **Страница статистики**: `http://localhost:8085/stats`
- **Health**: `http://localhost:8085/health`, `http://localhost:8085/api/health`, `http://localhost:8085/healthz`
- **База данных**: localhost:5432

## 📁 Структура проекта

```
ChipDip/
├── chipdip_parser/          # Основной модуль парсера
│   ├── app.py              # Главный файл парсера
│   ├── config_loader.py    # Загрузка конфигурации
│   ├── database.py         # Работа с базой данных
│   ├── scraper.py          # Логика парсинга
│   ├── scheduler.py        # Планировщик задач
│   └── utils.py            # Утилиты и логирование
├── webapp/                 # Flask веб-приложение
│   ├── __init__.py         # Создание Flask приложения
│   ├── routes.py           # Маршруты веб-интерфейса
│   ├── templates/          # HTML шаблоны
│   └── static/             # Статические файлы
├── docker-compose.yml      # Конфигурация Docker
├── Dockerfile              # Образ Docker
├── init.sql               # Инициализация БД
├── requirements.txt       # Python зависимости
├── TECH_SPEC.md           # Техническое задание
├── INSTALLATION.md        # Инструкция по установке и использованию
├── ARCHITECTURE.md        # Архитектура, модули, БД
└── .env                   # Конфигурация (создать из env.example)
```

## ⚙️ Конфигурация

### Основные переменные окружения (.env)

```bash
# База данных
DB_NAME=chipdip_db
DB_USER=chipdip_user
DB_PASSWORD=your_secure_password
DB_HOST=postgres
DB_PORT=5432

# Selenium/Firefox
GECKODRIVER_PATH=geckodriver
HEADLESS=1
SELENIUM_WAIT_TIMEOUT=25

# Парсер
MAX_REQUESTS_PER_HOUR=25
MIN_REQUEST_DELAY_SEC=70
WORK_START_HOUR=8
WORK_END_HOUR=17

# XPath селекторы (настройте под ваш сайт)
XPATH_STOCK=//div[@class='stock-info']//span[@class='quantity']
XPATH_PRODUCT_NAME=//h1[@class='product-title']
XPATH_PRICE=//span[@class='price-value']
```

## 🔧 Разработка

### Локальный запуск без Docker

1. **Установка зависимостей**
```bash
python -m venv venv
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows
pip install -r requirements.txt
```

2. **Настройка PostgreSQL**
```bash
# Установите PostgreSQL и создайте базу данных
createdb chipdip_db
psql -d chipdip_db -f init.sql
```

3. **Запуск приложений**
```bash
# Запуск веб-приложения
python run_webapp.py

# Запуск парсера (в отдельном терминале)
python -m chipdip_parser.app
```

## 📊 Использование

### Веб-интерфейс

1. **Главная страница**: Просмотр всех товаров с последними данными
2. **Добавление товара**: Добавление новых товаров для парсинга
3. **История товара**: Просмотр истории изменений конкретного товара

### API (основные)

- Публичные данные выручки:
  - `GET /api/stats/revenue/monthly`
  - `GET /api/stats/revenue/product-monthly?product_id=...`
- Экспорт:
  - `GET /api/export/chart/revenue/excel`
  - `GET /api/export/chart/product-revenue/excel?product_id=...`
- Полный список эндпоинтов и параметров — см. `ARCHITECTURE.md`.

## 🔍 Мониторинг

### Логи
```bash
# Логи всех сервисов
docker-compose logs -f

# Логи конкретного сервиса
docker-compose logs -f --tail=200 webapp
docker-compose logs -f parser
docker-compose logs -f postgres
```

### База данных
```bash
# Подключение к БД
docker-compose exec postgres psql -U chipdip_user -d chipdip_db

# Полезные запросы
SELECT COUNT(*) FROM products WHERE is_active = true;
SELECT * FROM products_with_latest_stock LIMIT 10;
```

## 🚨 Устранение неполадок

### Проблемы с парсером
1. **Ошибки Selenium**: Проверьте установку Firefox и geckodriver
2. **Таймауты**: Увеличьте `SELENIUM_WAIT_TIMEOUT` в .env
3. **Блокировка сайта**: Измените User-Agent или увеличьте задержки

### Проблемы с базой данных
1. **Ошибки подключения**: Проверьте настройки в .env
2. **Миграции**: Запустите `init.sql` для создания таблиц

### Проблемы с Docker
1. **Порты заняты**: Измените порты в docker-compose.yml
2. **Недостаточно памяти**: Увеличьте `shm_size` для парсера
3. **Отсутствуют новые маршруты/поведение**: пересоздайте контейнер `webapp`:
```bash
docker-compose up -d --force-recreate --no-deps webapp
```

## 📚 Дополнительные документы
- `TECH_SPEC.md` — полное ТЗ
- `INSTALLATION.md` — установка, запуск, использование
- `ARCHITECTURE.md` — описание модулей, БД, API

## 📝 Лицензия

Этот проект предназначен для образовательных целей. Убедитесь, что вы соблюдаете условия использования сайта ChipDip.

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории с подробным описанием ошибки и логами.
